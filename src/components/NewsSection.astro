---
import TimelineItem from './TimelineItem.astro';
import { events } from '../data/timeline';
---
<section id="news" class="pt-[140px] pb-[80px] max-[768px]:pt-[180px] relative overflow-hidden">

  <!-- Solid background — fades in on scroll, hides video so backdrop-filter is cheap -->
  <div id="news-bg" aria-hidden="true"
       style="position:absolute;inset:0;background:linear-gradient(to bottom,rgba(3,47,82,0) 0%,#032f52 160px,#032f52 calc(100% - 130px),rgba(3,47,82,0) 100%);opacity:0;transition:opacity 1.1s ease;pointer-events:none;z-index:0;"></div>

  <!-- Particle canvas (desktop only, driven by JS) -->
  <canvas id="news-particles" aria-hidden="true"
          style="position:absolute;inset:0;pointer-events:none;z-index:1;"></canvas>

  <!-- All content above bg + canvas -->
  <div style="position:relative;z-index:2;">

    <p data-animate class="mb-4 text-center text-[0.72rem] font-semibold uppercase tracking-[0.26em] text-brand-sky">
      <span data-i18n="no">Milepæler</span><span data-i18n="en">Milestones</span>
    </p>
    <h2 data-animate class="m-0 mb-4 text-center font-display text-[clamp(1.9rem,3.4vw,2.8rem)] font-bold leading-[1.1] tracking-[-0.02em] text-brand-white">
      <span data-i18n="no">Vår tidslinje</span><span data-i18n="en">Our timeline</span>
    </h2>
    <p data-animate class="mb-[70px] text-center text-sm tracking-wide text-white/35">
      <span data-i18n="no">Scroll ned for å gå tilbake i tid ↓</span><span data-i18n="en">Scroll down to travel back in time ↓</span>
    </p>

    <div class="timeline-container">
      <div class="timeline-track" aria-hidden="true"></div>

      {events.map((event, index) => (
        <TimelineItem
          date={event.date}
          dateEn={event.dateEn}
          logo={event.logo}
          logoAlt={event.logoAlt}
          title={event.title}
          titleEn={event.titleEn}
          content={event.content}
          contentEn={event.contentEn}
          logoStyle={event.logoStyle}
          side={index % 2 === 0 ? 'left' : 'right'}
        />
      ))}
    </div>

  </div>
</section>
